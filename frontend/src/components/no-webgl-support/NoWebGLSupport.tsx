export const NoWebGLSupport = () => {
	return (
		<p>
			This application required WebGL to run properly. Please enable WebGL in your browser
			settings, or use hardware which supports WebGL.
		</p>
	);
};
